class VaultService {
  const VaultService();

  String get statusHeadline => 'Vault setup pending';

  String get statusBody =>
      'Secure storage, biometric lock, and encrypted notes will live here once the vault flow is connected.';
}
